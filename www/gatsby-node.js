/*
exports.modifyWebpackConfig = ({ config }) => {
    config.loader("typescript", {
        test : /\.tsx?$/,
        loaders : [
            "awesome-typescript-loader"
        ]
    });

    return config;
}
*/
